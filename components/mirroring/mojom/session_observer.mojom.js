// components/mirroring/mojom/session_observer.mojom.js is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2014 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

'use strict';

(function() {
  var mojomId = 'components/mirroring/mojom/session_observer.mojom';
  if (mojo.internal.isMojomLoaded(mojomId)) {
    console.warn('The following mojom is loaded multiple times: ' + mojomId);
    return;
  }
  mojo.internal.markMojomLoaded(mojomId);
  var bindings = mojo;
  var associatedBindings = mojo;
  var codec = mojo.internal;
  var validator = mojo.internal;

  var exports = mojo.internal.exposeNamespace('mirroring.mojom');


  var SessionError = {};
  SessionError.ANSWER_TIME_OUT = 0;
  SessionError.ANSWER_NOT_OK = 1;
  SessionError.ANSWER_MISMATCHED_CAST_MODE = 2;
  SessionError.ANSWER_MISMATCHED_SSRC_LENGTH = 3;
  SessionError.ANSWER_SELECT_MULTIPLE_AUDIO = 4;
  SessionError.ANSWER_SELECT_MULTIPLE_VIDEO = 5;
  SessionError.ANSWER_SELECT_INVALID_INDEX = 6;
  SessionError.ANSWER_NO_AUDIO_OR_VIDEO = 7;
  SessionError.AUDIO_CAPTURE_ERROR = 8;
  SessionError.VIDEO_CAPTURE_ERROR = 9;
  SessionError.RTP_STREAM_ERROR = 10;
  SessionError.ENCODING_ERROR = 11;
  SessionError.CAST_TRANSPORT_ERROR = 12;
  SessionError.OPENSCREEN_SESSION_ERROR = 13;
  SessionError.MIN_VALUE = 0;
  SessionError.MAX_VALUE = 13;

  SessionError.isKnownEnumValue = function(value) {
    switch (value) {
    case 0:
    case 1:
    case 2:
    case 3:
    case 4:
    case 5:
    case 6:
    case 7:
    case 8:
    case 9:
    case 10:
    case 11:
    case 12:
    case 13:
      return true;
    }
    return false;
  };

  SessionError.toKnownEnumValue = function(value) {
    return value;
  };

  SessionError.validate = function(enumValue) {
    const isExtensible = false;
    if (isExtensible || this.isKnownEnumValue(enumValue))
      return validator.validationError.NONE;

    return validator.validationError.UNKNOWN_ENUM_VALUE;
  };

  function SessionObserver_OnError_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  SessionObserver_OnError_Params.prototype.initDefaults_ = function() {
    this.error = 0;
  };
  SessionObserver_OnError_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  SessionObserver_OnError_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    // validate SessionObserver_OnError_Params.error
    err = messageValidator.validateEnum(offset + codec.kStructHeaderSize + 0, SessionError);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  SessionObserver_OnError_Params.encodedSize = codec.kStructHeaderSize + 8;

  SessionObserver_OnError_Params.decode = function(decoder) {
    var packed;
    var val = new SessionObserver_OnError_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    val.error =
        decoder.decodeStruct(new codec.Enum(SessionError));
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    return val;
  };

  SessionObserver_OnError_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(SessionObserver_OnError_Params.encodedSize);
    encoder.writeUint32(0);
    encoder.encodeStruct(codec.Int32, val.error);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
  };
  function SessionObserver_DidStart_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  SessionObserver_DidStart_Params.prototype.initDefaults_ = function() {
  };
  SessionObserver_DidStart_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  SessionObserver_DidStart_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 8}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  SessionObserver_DidStart_Params.encodedSize = codec.kStructHeaderSize + 0;

  SessionObserver_DidStart_Params.decode = function(decoder) {
    var packed;
    var val = new SessionObserver_DidStart_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    return val;
  };

  SessionObserver_DidStart_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(SessionObserver_DidStart_Params.encodedSize);
    encoder.writeUint32(0);
  };
  function SessionObserver_DidStop_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  SessionObserver_DidStop_Params.prototype.initDefaults_ = function() {
  };
  SessionObserver_DidStop_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  SessionObserver_DidStop_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 8}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  SessionObserver_DidStop_Params.encodedSize = codec.kStructHeaderSize + 0;

  SessionObserver_DidStop_Params.decode = function(decoder) {
    var packed;
    var val = new SessionObserver_DidStop_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    return val;
  };

  SessionObserver_DidStop_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(SessionObserver_DidStop_Params.encodedSize);
    encoder.writeUint32(0);
  };
  function SessionObserver_LogInfoMessage_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  SessionObserver_LogInfoMessage_Params.prototype.initDefaults_ = function() {
    this.message = null;
  };
  SessionObserver_LogInfoMessage_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  SessionObserver_LogInfoMessage_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    // validate SessionObserver_LogInfoMessage_Params.message
    err = messageValidator.validateStringPointer(offset + codec.kStructHeaderSize + 0, false)
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  SessionObserver_LogInfoMessage_Params.encodedSize = codec.kStructHeaderSize + 8;

  SessionObserver_LogInfoMessage_Params.decode = function(decoder) {
    var packed;
    var val = new SessionObserver_LogInfoMessage_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    val.message =
        decoder.decodeStruct(codec.String);
    return val;
  };

  SessionObserver_LogInfoMessage_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(SessionObserver_LogInfoMessage_Params.encodedSize);
    encoder.writeUint32(0);
    encoder.encodeStruct(codec.String, val.message);
  };
  function SessionObserver_LogErrorMessage_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  SessionObserver_LogErrorMessage_Params.prototype.initDefaults_ = function() {
    this.message = null;
  };
  SessionObserver_LogErrorMessage_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  SessionObserver_LogErrorMessage_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    // validate SessionObserver_LogErrorMessage_Params.message
    err = messageValidator.validateStringPointer(offset + codec.kStructHeaderSize + 0, false)
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  SessionObserver_LogErrorMessage_Params.encodedSize = codec.kStructHeaderSize + 8;

  SessionObserver_LogErrorMessage_Params.decode = function(decoder) {
    var packed;
    var val = new SessionObserver_LogErrorMessage_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    val.message =
        decoder.decodeStruct(codec.String);
    return val;
  };

  SessionObserver_LogErrorMessage_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(SessionObserver_LogErrorMessage_Params.encodedSize);
    encoder.writeUint32(0);
    encoder.encodeStruct(codec.String, val.message);
  };
  function SessionObserver_OnSourceChanged_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  SessionObserver_OnSourceChanged_Params.prototype.initDefaults_ = function() {
  };
  SessionObserver_OnSourceChanged_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  SessionObserver_OnSourceChanged_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 8}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  SessionObserver_OnSourceChanged_Params.encodedSize = codec.kStructHeaderSize + 0;

  SessionObserver_OnSourceChanged_Params.decode = function(decoder) {
    var packed;
    var val = new SessionObserver_OnSourceChanged_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    return val;
  };

  SessionObserver_OnSourceChanged_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(SessionObserver_OnSourceChanged_Params.encodedSize);
    encoder.writeUint32(0);
  };
  function SessionObserver_OnRemotingStateChanged_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  SessionObserver_OnRemotingStateChanged_Params.prototype.initDefaults_ = function() {
    this.isRemoting = false;
  };
  SessionObserver_OnRemotingStateChanged_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  SessionObserver_OnRemotingStateChanged_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    return validator.validationError.NONE;
  };

  SessionObserver_OnRemotingStateChanged_Params.encodedSize = codec.kStructHeaderSize + 8;

  SessionObserver_OnRemotingStateChanged_Params.decode = function(decoder) {
    var packed;
    var val = new SessionObserver_OnRemotingStateChanged_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    packed = decoder.readUint8();
    val.isRemoting = (packed >> 0) & 1 ? true : false;
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    return val;
  };

  SessionObserver_OnRemotingStateChanged_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(SessionObserver_OnRemotingStateChanged_Params.encodedSize);
    encoder.writeUint32(0);
    packed = 0;
    packed |= (val.isRemoting & 1) << 0
    encoder.writeUint8(packed);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
  };
  var kSessionObserver_OnError_Name = 437688689;
  var kSessionObserver_DidStart_Name = 649824113;
  var kSessionObserver_DidStop_Name = 220255286;
  var kSessionObserver_LogInfoMessage_Name = 2072923446;
  var kSessionObserver_LogErrorMessage_Name = 40022048;
  var kSessionObserver_OnSourceChanged_Name = 349837390;
  var kSessionObserver_OnRemotingStateChanged_Name = 1501728672;

  function SessionObserverPtr(handleOrPtrInfo) {
    this.ptr = new bindings.InterfacePtrController(SessionObserver,
                                                   handleOrPtrInfo);
  }

  function SessionObserverAssociatedPtr(associatedInterfacePtrInfo) {
    this.ptr = new associatedBindings.AssociatedInterfacePtrController(
        SessionObserver, associatedInterfacePtrInfo);
  }

  SessionObserverAssociatedPtr.prototype =
      Object.create(SessionObserverPtr.prototype);
  SessionObserverAssociatedPtr.prototype.constructor =
      SessionObserverAssociatedPtr;

  function SessionObserverProxy(receiver) {
    this.receiver_ = receiver;
  }
  SessionObserverPtr.prototype.onError = function() {
    return SessionObserverProxy.prototype.onError
        .apply(this.ptr.getProxy(), arguments);
  };

  SessionObserverProxy.prototype.onError = function(error) {
    var params_ = new SessionObserver_OnError_Params();
    params_.error = error;
    var builder = new codec.MessageV0Builder(
        kSessionObserver_OnError_Name,
        codec.align(SessionObserver_OnError_Params.encodedSize));
    builder.encodeStruct(SessionObserver_OnError_Params, params_);
    var message = builder.finish();
    this.receiver_.accept(message);
  };
  SessionObserverPtr.prototype.didStart = function() {
    return SessionObserverProxy.prototype.didStart
        .apply(this.ptr.getProxy(), arguments);
  };

  SessionObserverProxy.prototype.didStart = function() {
    var params_ = new SessionObserver_DidStart_Params();
    var builder = new codec.MessageV0Builder(
        kSessionObserver_DidStart_Name,
        codec.align(SessionObserver_DidStart_Params.encodedSize));
    builder.encodeStruct(SessionObserver_DidStart_Params, params_);
    var message = builder.finish();
    this.receiver_.accept(message);
  };
  SessionObserverPtr.prototype.didStop = function() {
    return SessionObserverProxy.prototype.didStop
        .apply(this.ptr.getProxy(), arguments);
  };

  SessionObserverProxy.prototype.didStop = function() {
    var params_ = new SessionObserver_DidStop_Params();
    var builder = new codec.MessageV0Builder(
        kSessionObserver_DidStop_Name,
        codec.align(SessionObserver_DidStop_Params.encodedSize));
    builder.encodeStruct(SessionObserver_DidStop_Params, params_);
    var message = builder.finish();
    this.receiver_.accept(message);
  };
  SessionObserverPtr.prototype.logInfoMessage = function() {
    return SessionObserverProxy.prototype.logInfoMessage
        .apply(this.ptr.getProxy(), arguments);
  };

  SessionObserverProxy.prototype.logInfoMessage = function(message) {
    var params_ = new SessionObserver_LogInfoMessage_Params();
    params_.message = message;
    var builder = new codec.MessageV0Builder(
        kSessionObserver_LogInfoMessage_Name,
        codec.align(SessionObserver_LogInfoMessage_Params.encodedSize));
    builder.encodeStruct(SessionObserver_LogInfoMessage_Params, params_);
    var message = builder.finish();
    this.receiver_.accept(message);
  };
  SessionObserverPtr.prototype.logErrorMessage = function() {
    return SessionObserverProxy.prototype.logErrorMessage
        .apply(this.ptr.getProxy(), arguments);
  };

  SessionObserverProxy.prototype.logErrorMessage = function(message) {
    var params_ = new SessionObserver_LogErrorMessage_Params();
    params_.message = message;
    var builder = new codec.MessageV0Builder(
        kSessionObserver_LogErrorMessage_Name,
        codec.align(SessionObserver_LogErrorMessage_Params.encodedSize));
    builder.encodeStruct(SessionObserver_LogErrorMessage_Params, params_);
    var message = builder.finish();
    this.receiver_.accept(message);
  };
  SessionObserverPtr.prototype.onSourceChanged = function() {
    return SessionObserverProxy.prototype.onSourceChanged
        .apply(this.ptr.getProxy(), arguments);
  };

  SessionObserverProxy.prototype.onSourceChanged = function() {
    var params_ = new SessionObserver_OnSourceChanged_Params();
    var builder = new codec.MessageV0Builder(
        kSessionObserver_OnSourceChanged_Name,
        codec.align(SessionObserver_OnSourceChanged_Params.encodedSize));
    builder.encodeStruct(SessionObserver_OnSourceChanged_Params, params_);
    var message = builder.finish();
    this.receiver_.accept(message);
  };
  SessionObserverPtr.prototype.onRemotingStateChanged = function() {
    return SessionObserverProxy.prototype.onRemotingStateChanged
        .apply(this.ptr.getProxy(), arguments);
  };

  SessionObserverProxy.prototype.onRemotingStateChanged = function(isRemoting) {
    var params_ = new SessionObserver_OnRemotingStateChanged_Params();
    params_.isRemoting = isRemoting;
    var builder = new codec.MessageV0Builder(
        kSessionObserver_OnRemotingStateChanged_Name,
        codec.align(SessionObserver_OnRemotingStateChanged_Params.encodedSize));
    builder.encodeStruct(SessionObserver_OnRemotingStateChanged_Params, params_);
    var message = builder.finish();
    this.receiver_.accept(message);
  };

  function SessionObserverStub(delegate) {
    this.delegate_ = delegate;
  }
  SessionObserverStub.prototype.onError = function(error) {
    return this.delegate_ && this.delegate_.onError && this.delegate_.onError(error);
  }
  SessionObserverStub.prototype.didStart = function() {
    return this.delegate_ && this.delegate_.didStart && this.delegate_.didStart();
  }
  SessionObserverStub.prototype.didStop = function() {
    return this.delegate_ && this.delegate_.didStop && this.delegate_.didStop();
  }
  SessionObserverStub.prototype.logInfoMessage = function(message) {
    return this.delegate_ && this.delegate_.logInfoMessage && this.delegate_.logInfoMessage(message);
  }
  SessionObserverStub.prototype.logErrorMessage = function(message) {
    return this.delegate_ && this.delegate_.logErrorMessage && this.delegate_.logErrorMessage(message);
  }
  SessionObserverStub.prototype.onSourceChanged = function() {
    return this.delegate_ && this.delegate_.onSourceChanged && this.delegate_.onSourceChanged();
  }
  SessionObserverStub.prototype.onRemotingStateChanged = function(isRemoting) {
    return this.delegate_ && this.delegate_.onRemotingStateChanged && this.delegate_.onRemotingStateChanged(isRemoting);
  }

  SessionObserverStub.prototype.accept = function(message) {
    var reader = new codec.MessageReader(message);
    switch (reader.messageName) {
    case kSessionObserver_OnError_Name:
      var params = reader.decodeStruct(SessionObserver_OnError_Params);
      this.onError(params.error);
      return true;
    case kSessionObserver_DidStart_Name:
      var params = reader.decodeStruct(SessionObserver_DidStart_Params);
      this.didStart();
      return true;
    case kSessionObserver_DidStop_Name:
      var params = reader.decodeStruct(SessionObserver_DidStop_Params);
      this.didStop();
      return true;
    case kSessionObserver_LogInfoMessage_Name:
      var params = reader.decodeStruct(SessionObserver_LogInfoMessage_Params);
      this.logInfoMessage(params.message);
      return true;
    case kSessionObserver_LogErrorMessage_Name:
      var params = reader.decodeStruct(SessionObserver_LogErrorMessage_Params);
      this.logErrorMessage(params.message);
      return true;
    case kSessionObserver_OnSourceChanged_Name:
      var params = reader.decodeStruct(SessionObserver_OnSourceChanged_Params);
      this.onSourceChanged();
      return true;
    case kSessionObserver_OnRemotingStateChanged_Name:
      var params = reader.decodeStruct(SessionObserver_OnRemotingStateChanged_Params);
      this.onRemotingStateChanged(params.isRemoting);
      return true;
    default:
      return false;
    }
  };

  SessionObserverStub.prototype.acceptWithResponder =
      function(message, responder) {
    var reader = new codec.MessageReader(message);
    switch (reader.messageName) {
    default:
      return false;
    }
  };

  function validateSessionObserverRequest(messageValidator) {
    var message = messageValidator.message;
    var paramsClass = null;
    switch (message.getName()) {
      case kSessionObserver_OnError_Name:
        if (!message.expectsResponse() && !message.isResponse())
          paramsClass = SessionObserver_OnError_Params;
      break;
      case kSessionObserver_DidStart_Name:
        if (!message.expectsResponse() && !message.isResponse())
          paramsClass = SessionObserver_DidStart_Params;
      break;
      case kSessionObserver_DidStop_Name:
        if (!message.expectsResponse() && !message.isResponse())
          paramsClass = SessionObserver_DidStop_Params;
      break;
      case kSessionObserver_LogInfoMessage_Name:
        if (!message.expectsResponse() && !message.isResponse())
          paramsClass = SessionObserver_LogInfoMessage_Params;
      break;
      case kSessionObserver_LogErrorMessage_Name:
        if (!message.expectsResponse() && !message.isResponse())
          paramsClass = SessionObserver_LogErrorMessage_Params;
      break;
      case kSessionObserver_OnSourceChanged_Name:
        if (!message.expectsResponse() && !message.isResponse())
          paramsClass = SessionObserver_OnSourceChanged_Params;
      break;
      case kSessionObserver_OnRemotingStateChanged_Name:
        if (!message.expectsResponse() && !message.isResponse())
          paramsClass = SessionObserver_OnRemotingStateChanged_Params;
      break;
    }
    if (paramsClass === null)
      return validator.validationError.NONE;
    return paramsClass.validate(messageValidator, messageValidator.message.getHeaderNumBytes());
  }

  function validateSessionObserverResponse(messageValidator) {
    return validator.validationError.NONE;
  }

  var SessionObserver = {
    name: 'mirroring.mojom.SessionObserver',
    kVersion: 0,
    ptrClass: SessionObserverPtr,
    proxyClass: SessionObserverProxy,
    stubClass: SessionObserverStub,
    validateRequest: validateSessionObserverRequest,
    validateResponse: null,
  };
  SessionObserverStub.prototype.validator = validateSessionObserverRequest;
  SessionObserverProxy.prototype.validator = null;
  exports.SessionError = SessionError;
  exports.SessionObserver = SessionObserver;
  exports.SessionObserverPtr = SessionObserverPtr;
  exports.SessionObserverAssociatedPtr = SessionObserverAssociatedPtr;
})();