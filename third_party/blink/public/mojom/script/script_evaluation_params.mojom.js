// third_party/blink/public/mojom/script/script_evaluation_params.mojom.js is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2014 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

'use strict';

(function() {
  var mojomId = 'third_party/blink/public/mojom/script/script_evaluation_params.mojom';
  if (mojo.internal.isMojomLoaded(mojomId)) {
    console.warn('The following mojom is loaded multiple times: ' + mojomId);
    return;
  }
  mojo.internal.markMojomLoaded(mojomId);
  var bindings = mojo;
  var associatedBindings = mojo;
  var codec = mojo.internal;
  var validator = mojo.internal;

  var exports = mojo.internal.exposeNamespace('blink.mojom');


  var UserActivationOption = {};
  UserActivationOption.kDoNotActivate = 0;
  UserActivationOption.kActivate = 1;
  UserActivationOption.MIN_VALUE = 0;
  UserActivationOption.MAX_VALUE = 1;

  UserActivationOption.isKnownEnumValue = function(value) {
    switch (value) {
    case 0:
    case 1:
      return true;
    }
    return false;
  };

  UserActivationOption.toKnownEnumValue = function(value) {
    return value;
  };

  UserActivationOption.validate = function(enumValue) {
    const isExtensible = false;
    if (isExtensible || this.isKnownEnumValue(enumValue))
      return validator.validationError.NONE;

    return validator.validationError.UNKNOWN_ENUM_VALUE;
  };
  var WantResultOption = {};
  WantResultOption.kNoResult = 0;
  WantResultOption.kWantResult = 1;
  WantResultOption.kWantResultDateAndRegExpAllowed = 2;
  WantResultOption.MIN_VALUE = 0;
  WantResultOption.MAX_VALUE = 2;

  WantResultOption.isKnownEnumValue = function(value) {
    switch (value) {
    case 0:
    case 1:
    case 2:
      return true;
    }
    return false;
  };

  WantResultOption.toKnownEnumValue = function(value) {
    return value;
  };

  WantResultOption.validate = function(enumValue) {
    const isExtensible = false;
    if (isExtensible || this.isKnownEnumValue(enumValue))
      return validator.validationError.NONE;

    return validator.validationError.UNKNOWN_ENUM_VALUE;
  };
  var PromiseResultOption = {};
  PromiseResultOption.kAwait = 0;
  PromiseResultOption.kDoNotWait = 1;
  PromiseResultOption.MIN_VALUE = 0;
  PromiseResultOption.MAX_VALUE = 1;

  PromiseResultOption.isKnownEnumValue = function(value) {
    switch (value) {
    case 0:
    case 1:
      return true;
    }
    return false;
  };

  PromiseResultOption.toKnownEnumValue = function(value) {
    return value;
  };

  PromiseResultOption.validate = function(enumValue) {
    const isExtensible = false;
    if (isExtensible || this.isKnownEnumValue(enumValue))
      return validator.validationError.NONE;

    return validator.validationError.UNKNOWN_ENUM_VALUE;
  };
  var EvaluationTiming = {};
  EvaluationTiming.kAsynchronous = 0;
  EvaluationTiming.kSynchronous = 1;
  EvaluationTiming.MIN_VALUE = 0;
  EvaluationTiming.MAX_VALUE = 1;

  EvaluationTiming.isKnownEnumValue = function(value) {
    switch (value) {
    case 0:
    case 1:
      return true;
    }
    return false;
  };

  EvaluationTiming.toKnownEnumValue = function(value) {
    return value;
  };

  EvaluationTiming.validate = function(enumValue) {
    const isExtensible = false;
    if (isExtensible || this.isKnownEnumValue(enumValue))
      return validator.validationError.NONE;

    return validator.validationError.UNKNOWN_ENUM_VALUE;
  };
  var LoadEventBlockingOption = {};
  LoadEventBlockingOption.kDoNotBlock = 0;
  LoadEventBlockingOption.kBlock = 1;
  LoadEventBlockingOption.MIN_VALUE = 0;
  LoadEventBlockingOption.MAX_VALUE = 1;

  LoadEventBlockingOption.isKnownEnumValue = function(value) {
    switch (value) {
    case 0:
    case 1:
      return true;
    }
    return false;
  };

  LoadEventBlockingOption.toKnownEnumValue = function(value) {
    return value;
  };

  LoadEventBlockingOption.validate = function(enumValue) {
    const isExtensible = false;
    if (isExtensible || this.isKnownEnumValue(enumValue))
      return validator.validationError.NONE;

    return validator.validationError.UNKNOWN_ENUM_VALUE;
  };

  exports.UserActivationOption = UserActivationOption;
  exports.WantResultOption = WantResultOption;
  exports.PromiseResultOption = PromiseResultOption;
  exports.EvaluationTiming = EvaluationTiming;
  exports.LoadEventBlockingOption = LoadEventBlockingOption;
})();