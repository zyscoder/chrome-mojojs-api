// chrome/common/metrics.mojom.js is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2014 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

'use strict';

(function() {
  var mojomId = 'chrome/common/metrics.mojom';
  if (mojo.internal.isMojomLoaded(mojomId)) {
    console.warn('The following mojom is loaded multiple times: ' + mojomId);
    return;
  }
  mojo.internal.markMojomLoaded(mojomId);
  var bindings = mojo;
  var associatedBindings = mojo;
  var codec = mojo.internal;
  var validator = mojo.internal;

  var exports = mojo.internal.exposeNamespace('chrome.mojom');



  function MetricsService_IsMetricsAndCrashReportingEnabled_Params(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  MetricsService_IsMetricsAndCrashReportingEnabled_Params.prototype.initDefaults_ = function() {
  };
  MetricsService_IsMetricsAndCrashReportingEnabled_Params.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  MetricsService_IsMetricsAndCrashReportingEnabled_Params.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 8}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;

    return validator.validationError.NONE;
  };

  MetricsService_IsMetricsAndCrashReportingEnabled_Params.encodedSize = codec.kStructHeaderSize + 0;

  MetricsService_IsMetricsAndCrashReportingEnabled_Params.decode = function(decoder) {
    var packed;
    var val = new MetricsService_IsMetricsAndCrashReportingEnabled_Params();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    return val;
  };

  MetricsService_IsMetricsAndCrashReportingEnabled_Params.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(MetricsService_IsMetricsAndCrashReportingEnabled_Params.encodedSize);
    encoder.writeUint32(0);
  };
  function MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams(values) {
    this.initDefaults_();
    this.initFields_(values);
  }


  MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.prototype.initDefaults_ = function() {
    this.result = false;
  };
  MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.prototype.initFields_ = function(fields) {
    for(var field in fields) {
        if (this.hasOwnProperty(field))
          this[field] = fields[field];
    }
  };

  MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.validate = function(messageValidator, offset) {
    var err;
    err = messageValidator.validateStructHeader(offset, codec.kStructHeaderSize);
    if (err !== validator.validationError.NONE)
        return err;

    var kVersionSizes = [
      {version: 0, numBytes: 16}
    ];
    err = messageValidator.validateStructVersion(offset, kVersionSizes);
    if (err !== validator.validationError.NONE)
        return err;


    return validator.validationError.NONE;
  };

  MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.encodedSize = codec.kStructHeaderSize + 8;

  MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.decode = function(decoder) {
    var packed;
    var val = new MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams();
    var numberOfBytes = decoder.readUint32();
    var version = decoder.readUint32();
    packed = decoder.readUint8();
    val.result = (packed >> 0) & 1 ? true : false;
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    decoder.skip(1);
    return val;
  };

  MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.encode = function(encoder, val) {
    var packed;
    encoder.writeUint32(MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.encodedSize);
    encoder.writeUint32(0);
    packed = 0;
    packed |= (val.result & 1) << 0
    encoder.writeUint8(packed);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
    encoder.skip(1);
  };
  var kMetricsService_IsMetricsAndCrashReportingEnabled_Name = 259103858;

  function MetricsServicePtr(handleOrPtrInfo) {
    this.ptr = new bindings.InterfacePtrController(MetricsService,
                                                   handleOrPtrInfo);
  }

  function MetricsServiceAssociatedPtr(associatedInterfacePtrInfo) {
    this.ptr = new associatedBindings.AssociatedInterfacePtrController(
        MetricsService, associatedInterfacePtrInfo);
  }

  MetricsServiceAssociatedPtr.prototype =
      Object.create(MetricsServicePtr.prototype);
  MetricsServiceAssociatedPtr.prototype.constructor =
      MetricsServiceAssociatedPtr;

  function MetricsServiceProxy(receiver) {
    this.receiver_ = receiver;
  }
  MetricsServicePtr.prototype.isMetricsAndCrashReportingEnabled = function() {
    return MetricsServiceProxy.prototype.isMetricsAndCrashReportingEnabled
        .apply(this.ptr.getProxy(), arguments);
  };

  MetricsServiceProxy.prototype.isMetricsAndCrashReportingEnabled = function() {
    var params_ = new MetricsService_IsMetricsAndCrashReportingEnabled_Params();
    return new Promise(function(resolve, reject) {
      var builder = new codec.MessageV1Builder(
          kMetricsService_IsMetricsAndCrashReportingEnabled_Name,
          codec.align(MetricsService_IsMetricsAndCrashReportingEnabled_Params.encodedSize),
          codec.kMessageExpectsResponse, 0);
      builder.encodeStruct(MetricsService_IsMetricsAndCrashReportingEnabled_Params, params_);
      var message = builder.finish();
      this.receiver_.acceptAndExpectResponse(message).then(function(message) {
        var reader = new codec.MessageReader(message);
        var responseParams =
            reader.decodeStruct(MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams);
        resolve(responseParams);
      }).catch(function(result) {
        reject(Error("Connection error: " + result));
      });
    }.bind(this));
  };

  function MetricsServiceStub(delegate) {
    this.delegate_ = delegate;
  }
  MetricsServiceStub.prototype.isMetricsAndCrashReportingEnabled = function() {
    return this.delegate_ && this.delegate_.isMetricsAndCrashReportingEnabled && this.delegate_.isMetricsAndCrashReportingEnabled();
  }

  MetricsServiceStub.prototype.accept = function(message) {
    var reader = new codec.MessageReader(message);
    switch (reader.messageName) {
    default:
      return false;
    }
  };

  MetricsServiceStub.prototype.acceptWithResponder =
      function(message, responder) {
    var reader = new codec.MessageReader(message);
    switch (reader.messageName) {
    case kMetricsService_IsMetricsAndCrashReportingEnabled_Name:
      var params = reader.decodeStruct(MetricsService_IsMetricsAndCrashReportingEnabled_Params);
      this.isMetricsAndCrashReportingEnabled().then(function(response) {
        var responseParams =
            new MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams();
        responseParams.result = response.result;
        var builder = new codec.MessageV1Builder(
            kMetricsService_IsMetricsAndCrashReportingEnabled_Name,
            codec.align(MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams.encodedSize),
            codec.kMessageIsResponse, reader.requestID);
        builder.encodeStruct(MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams,
                             responseParams);
        var message = builder.finish();
        responder.accept(message);
      });
      return true;
    default:
      return false;
    }
  };

  function validateMetricsServiceRequest(messageValidator) {
    var message = messageValidator.message;
    var paramsClass = null;
    switch (message.getName()) {
      case kMetricsService_IsMetricsAndCrashReportingEnabled_Name:
        if (message.expectsResponse())
          paramsClass = MetricsService_IsMetricsAndCrashReportingEnabled_Params;
      break;
    }
    if (paramsClass === null)
      return validator.validationError.NONE;
    return paramsClass.validate(messageValidator, messageValidator.message.getHeaderNumBytes());
  }

  function validateMetricsServiceResponse(messageValidator) {
   var message = messageValidator.message;
   var paramsClass = null;
   switch (message.getName()) {
      case kMetricsService_IsMetricsAndCrashReportingEnabled_Name:
        if (message.isResponse())
          paramsClass = MetricsService_IsMetricsAndCrashReportingEnabled_ResponseParams;
        break;
    }
    if (paramsClass === null)
      return validator.validationError.NONE;
    return paramsClass.validate(messageValidator, messageValidator.message.getHeaderNumBytes());
  }

  var MetricsService = {
    name: 'chrome.mojom.MetricsService',
    kVersion: 0,
    ptrClass: MetricsServicePtr,
    proxyClass: MetricsServiceProxy,
    stubClass: MetricsServiceStub,
    validateRequest: validateMetricsServiceRequest,
    validateResponse: validateMetricsServiceResponse,
  };
  MetricsServiceStub.prototype.validator = validateMetricsServiceRequest;
  MetricsServiceProxy.prototype.validator = validateMetricsServiceResponse;
  exports.MetricsService = MetricsService;
  exports.MetricsServicePtr = MetricsServicePtr;
  exports.MetricsServiceAssociatedPtr = MetricsServiceAssociatedPtr;
})();